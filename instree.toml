# Instree — configuration (éditable via l'interface web)
# Secrets : instree.local.toml (gitignored)
# n = nombre de mutuels suivis (nombre ou MAX = tous)

[instagram]
sessionid = ""
ds_user_id = ""

[scan]
username = ""
n = 100
watch_n = "MAX"
page_sleep = 0.6

[web]
host = "127.0.0.1"
port = 8765

[schedule]
times = ["08:00"]
