# Instree — configuration (éditable via l'interface web)
# Secrets : instree.local.toml (gitignored)
# n / watch_n : nombre ou MAX (= tous les abonnements)

[instagram]
sessionid = ""
ds_user_id = ""

[scan]
username = ""
n = "MAX"
watch_n = "MAX"
page_sleep = 0.6
page_size = 200

[web]
host = "127.0.0.1"
port = 8765

[schedule]
times = ["08:00"]
