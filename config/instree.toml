# Instree — configuration globale (éditable via l'interface web)
# Sessions : config/profiles/<id>/  (+ data/profiles/<id>/)

[web]
host = "127.0.0.1"
port = 8765
autostart_on_boot = false

[profiles]
active = "s-6843f880"
