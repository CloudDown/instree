# Lanceurs Instree

| Produit | Linux / macOS | Windows |
|---------|---------------|---------|
| **Desktop** | `./bin/run-desktop` | `bin\run-desktop.bat` |
| **Web** | `./bin/run-web` | `bin\run-web.bat` |

Données runtime : `var/desktop/` (Desktop) · `var/web/` (Web).
