# Secrets Instagram — copier vers var/desktop/config/instree.local.toml
# var/desktop/config/instree.local.toml est gitignored et surcharge instree.toml
#
# Récupération manuelle : README.md section « Session Instagram »
# (F12 → Application/Stockage → Cookies → instagram.com → sessionid + ds_user_id)

[instagram]
sessionid = ""
ds_user_id = ""
